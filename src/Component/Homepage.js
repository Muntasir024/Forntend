import Layout from './MainPage/Layout';
import NavBar from './NavBar/NavBar';

function Homepage() {
  return (
    <div>
        <NavBar />
        <Layout />
    </div>
  );
}

export default Homepage;
